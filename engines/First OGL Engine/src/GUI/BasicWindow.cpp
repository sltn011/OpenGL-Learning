#include "GUI/BasicWindow.hpp"

namespace OGL::E1::GUI {
    BasicWindow::BasicWindow(
        bool isEnabled
    ) : m_enabled{ isEnabled } {

    }

    BasicWindow::~BasicWindow(
    ) {

    }

} //OGL::E1::GUI